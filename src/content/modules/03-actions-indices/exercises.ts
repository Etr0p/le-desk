import type { ExerciseGenerator } from '../../../engine/types';

// Squelette du module 3 — les générateurs d'exercices seront ajoutés ici (patron : module 02).
// Invariant à respecter : tous les calculs passeront par calculs.ts, jamais de formule recopiée.
export const exercices: ExerciseGenerator[] = [];
