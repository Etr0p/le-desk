import type { ProblemGenerator } from '../../../engine/types';

// Squelette du module 3 — les moules de problèmes multi-étapes seront ajoutés ici (patron : module 02).
export const problemes: ProblemGenerator[] = [];
