import type { Flashcard } from '../../../engine/types';

// Squelette du module 7 — flashcards à venir (~120 cartes bilingues : définitions,
// formules, ordres de grandeur, pièges, réflexes jury, calculs mentaux).
export const flashcards: Flashcard[] = [];
