import type { Formule } from '../../../engine/types';

// Squelette du module 7 — formulaire à venir (LaTeX + commentaires bilingues,
// aligné sur les conventions documentées en tête de calculs.ts).
export const formules: Formule[] = [];
